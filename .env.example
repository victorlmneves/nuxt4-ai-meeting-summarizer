# Copy this file to .env and fill in your API keys.
# You only need the key(s) for the provider(s) you want to use.

# ── AI providers ─────────────────────────────────────────────────────────────

# Anthropic Claude — https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI GPT — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Google Gemini — https://aistudio.google.com/apikey  (free tier, no credit card required)
GEMINI_API_KEY=AIza...

# ── Database (Turso) ──────────────────────────────────────────────────────────
# Get these from https://turso.tech after creating a database.
# Leave empty in development — falls back to a local SQLite file at ./data/minutai.db
TURSO_DB_URL=libsql://your-db-name.turso.io
TURSO_AUTH_TOKEN=

# ── Authentication (Phase 2) ──────────────────────────────────────────────────
# Session password (min 32 characters) — generate with: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# IMPORTANT: In production, use a strong random string
NUXT_SESSION_PASSWORD=your-32-character-secret-here-min32chars

# GitHub OAuth — https://github.com/settings/developers
# 1. Create OAuth App at https://github.com/settings/developers
# 2. Set Authorization callback URL to: http://localhost:3000/api/auth/github (or your domain)
NUXT_OAUTH_GITHUB_CLIENT_ID=your_github_client_id
NUXT_OAUTH_GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth — https://console.cloud.google.com
# 1. Create OAuth 2.0 Client ID at https://console.cloud.google.com
# 2. Set Authorized redirect URIs to: http://localhost:3000/api/auth/google (or your domain)
NUXT_OAUTH_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=your_google_client_secret

# Public site URL (for OAuth redirect URIs) — set to your domain in production
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Integrations (Phase 3) ────────────────────────────────────────────────────
# These are saved via the UI at /integrations and stored in the database per-user.
# But you can optionally set defaults here. See INTEGRATION_SETUP.md for detailed instructions.

# JIRA
# JIRA_BASE_URL=https://your-domain.atlassian.net
# JIRA_EMAIL=your-email@example.com
# JIRA_API_TOKEN=your-api-token
# JIRA_PROJECT_KEY=PROJ

# LINEAR
# LINEAR_API_KEY=lin_xxx
# LINEAR_TEAM_ID=team_xxx

# NOTION
# NOTION_INTEGRATION_TOKEN=secret_xxx
# NOTION_DATABASE_ID=xxx

# AZURE DEVOPS
# AZURE_ORGANIZATION=my-org
# AZURE_PROJECT=my-project
# AZURE_PAT=your-personal-access-token

# ── Session (nuxt-auth-utils) ─────────────────────────────────────────────────
# Random string of at least 32 characters — used to encrypt session cookies.
# Generate one with: openssl rand -base64 32
NUXT_SESSION_PASSWORD=

# ── OAuth (Phase 4 — leave blank until auth is implemented) ──────────────────
# GitHub OAuth app: https://github.com/settings/developers
NUXT_OAUTH_GITHUB_CLIENT_ID=
NUXT_OAUTH_GITHUB_CLIENT_SECRET=

# Google OAuth app: https://console.cloud.google.com/apis/credentials
NUXT_OAUTH_GOOGLE_CLIENT_ID=
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=
